import React from 'react';
import { Card, PageHeader } from '../../components';

const Dashboard = () => {
  return (
    <Card className="page-content">
      <PageHeader title={`Welcome Tools Component Library`} />
    </Card>
  );
};

export default Dashboard;
